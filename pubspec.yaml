name: spacemaker
description: SpaceMaker — Swipe left for storage
publish_to: "none"
version: 1.1.0+3

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  photo_manager: ^3.6.3
  video_player: ^2.9.2
  intl: ^0.20.1
  purchases_flutter: 10.12.0
  google_mobile_ads: 9.1.0
  url_launcher: 6.3.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
